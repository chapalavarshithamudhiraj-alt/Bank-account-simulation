#include <stdio.h>
int main() {
    int balance = 1000, deposit;
    printf("Enter deposit amount: ");
    scanf("%d", &deposit);

    balance += deposit;
    printf("Balance = %d", balance);
    return 0;
}
